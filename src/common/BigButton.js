import React from 'react';

const bigButtonStyle = {
  display: "flex",
  alignItems: "middle"
}

const BigButton = (props) => {
  return (
    <button>
      
    </button>
  )
}

export default BigButton;